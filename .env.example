# Mapbox — the 3D dark map in the About section.
# Get a public token at https://account.mapbox.com/access-tokens
# Copy this file to .env.local (gitignored) and paste your token.
# Without a token, a procedural blueprint map is shown instead.
VITE_MAPBOX_TOKEN=
 